##思路
1. 抓包了解登录的详细流程,找到提交表单的URL，并获取POST所需参数，发现参数中含有一串类似乱码的值，应该是为防止CSRF的TOKEN，在登录页面的源码中可以提取到该值，所以需要首先请求登录页面，利用正则提取出该TOKEN。

2. 将POST所需数据准备好，进行提交，此时需要使用登录页面中保存的cookie。发现HTTP返回状态码422 ，无法处理的请求实体。使用火狐poster插件进行尝试，发现返回相同的状态码。插件中没有使用cookie，推测可能是cookie的问题。观察使用浏览器登录时的数据包，发现post提交数据后保存的cookie不全，此时与登录页面使用了同一个cookie文件，尝试创建新的cooie文件并将登录时的cookie保存在新的cookie文件中，此时返回状态码302，重定向，并且cookie中数据与浏览器登录时相同。

3. 使用登录时的cookie 请求个人中心页面，得到页面代码，利用正则提取出用户名称。
